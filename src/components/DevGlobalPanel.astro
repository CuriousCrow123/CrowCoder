---
// Dev-only wrapper: renders GlobalParamPanel only in dev mode.
// Astro needs a static import for renderer resolution — the DEV gate
// ensures this entire file is tree-shaken from production builds.
import GlobalParamPanel from '../lib/dev/GlobalParamPanel.svelte';
---

{import.meta.env.DEV && <GlobalParamPanel client:idle />}
